const signupHeader = (title) => {
    return `<header class="signup__header">
                <div class="header__title">
                    <h1 class="header__text">${title}</h1>
                </div>
            </header>`
};

export  {
    signupHeader,
}